# HIGH CVEs no sistema base Debian 13.5 - sem Fixed Version disponível
# linux-libc-dev - headers do kernel, não é o kernel em execução
# Migração para alpine eliminou CVEs Debian (linux-libc-dev, krb5, ncurses, curl) 

# krb5 - dependência do sistema base Debian 13.5
CVE-2026-40356

# ncurses - dependência do sistema base Debian 13.5
CVE-2025-69720